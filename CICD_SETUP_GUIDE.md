# CI/CD 自动部署配置指南 🚀

> **目标**：配置 GitHub Actions 自动部署后端到 Render
> **前置条件**：已完成 Render 手动部署
> **预计耗时**：10-15 分钟

---

## 📋 配置概览

配置完成后，您将获得：

✅ **自动部署**：推送代码到 main 分支自动触发部署
✅ **代码检查**：TypeScript 类型检查 + 构建验证
✅ **健康检查**：部署后自动测试所有关键端点
✅ **手动触发**：需要时可以手动触发部署

**工作流程**：
```
git push origin main
    ↓
GitHub Actions 检查代码质量
    ↓
触发 Render 部署
    ↓
等待部署完成
    ↓
运行健康检查
    ↓
✅ 部署完成！
```

---

## 🔑 第一步：获取 Render API Key

### 1.1 访问 Render Account Settings

1. 登录 Render：https://dashboard.render.com
2. 点击右上角的用户头像
3. 选择 **Account Settings**

```
┌──────────────────────────────────────┐
│  👤 Your Name               ▼        │
├──────────────────────────────────────┤
│  Dashboard                           │
│  Account Settings      ← 点击这里    │
│  Billing                             │
│  Log out                             │
└──────────────────────────────────────┘
```

### 1.2 创建 API Key

进入 Account Settings 后：

1. 在左侧菜单找到 **API Keys** 部分
2. 点击 **Create API Key** 按钮

```
┌─────────────────────────────────────────────────────────┐
│ Account Settings                                        │
├─────────────────────────────────────────────────────────┤
│ ◀ API Keys                                              │
│                                                         │
│ API keys allow you to programmatically deploy and      │
│ manage your services.                                  │
│                                                         │
│ ┌───────────────────────────────────────────────────┐ │
│ │ No API keys yet                                   │ │
│ │                                                   │ │
│ │ [Create API Key]          ← 点击这个按钮          │ │
│ └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 1.3 填写 API Key 信息

会弹出对话框：

```
┌──────────────────────────────────────┐
│ Create API Key                       │
├──────────────────────────────────────┤
│ Name: [GitHub Actions Deploy]        │
│                                      │
│ [Create API Key]  [Cancel]           │
└──────────────────────────────────────┘
```

**填写**：
- Name: `GitHub Actions Deploy`（或任何您喜欢的名称）

点击 **Create API Key**

### 1.4 复制 API Key

⚠️ **重要**：API Key 只会显示**一次**！

创建后会显示：

```
┌─────────────────────────────────────────────────────────┐
│ API Key Created                                         │
├─────────────────────────────────────────────────────────┤
│ Your API key has been created. Copy it now as it won't │
│ be shown again.                                         │
│                                                         │
│ ┌───────────────────────────────────────────────────┐ │
│ │ rnd_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx          │ │
│ │                                          [Copy]   │ │
│ └───────────────────────────────────────────────────┘ │
│                                                         │
│ [Done]                                                  │
└─────────────────────────────────────────────────────────┘
```

**操作**：
1. 点击 **[Copy]** 按钮复制 API Key
2. 将它粘贴到记事本保存（稍后会用到）
3. API Key 格式：`rnd_` 开头的长字符串

**示例**：
```
rnd_abc123def456ghi789jkl012mno345pqr678stu901
```

✅ **已获得**：`RENDER_API_KEY`

---

## 🆔 第二步：获取 Render Service ID

### 2.1 打开您的 Web Service

1. 回到 Render Dashboard：https://dashboard.render.com
2. 点击您的后端服务名称（例如：`apologize-backend`）

### 2.2 从 URL 中获取 Service ID

进入服务详情页后，查看浏览器地址栏：

```
https://dashboard.render.com/web/srv-xxxxxxxxxxxxx
                                    ↑
                          这就是 Service ID
```

**Service ID 格式**：
- 以 `srv-` 开头
- 后面跟 15 个随机字符（字母和数字）

**示例**：
```
srv-ctq8p8v79t8c73bqh8rg
```

**操作**：
1. 从浏览器地址栏复制完整的 Service ID（包括 `srv-` 前缀）
2. 粘贴到记事本保存

✅ **已获得**：`RENDER_SERVICE_ID`

---

## 🌐 第三步：获取 Render Service URL

### 3.1 查看服务 URL

在服务详情页顶部，您会看到：

```
┌─────────────────────────────────────────────────────────┐
│ apologize-backend                                       │
│ Status: ● Live                                          │
├─────────────────────────────────────────────────────────┤
│ https://apologize-backend-xxxx.onrender.com             │
│ [Copy URL]  [Open]                                      │
└─────────────────────────────────────────────────────────┘
```

**操作**：
1. 点击 **[Copy URL]** 复制完整 URL
2. 粘贴到记事本保存

**示例**：
```
https://apologize-backend-ctq8.onrender.com
```

⚠️ **重要**：URL 末尾**不要**有斜杠 `/`

✅ **已获得**：`RENDER_SERVICE_URL`

---

## 🔐 第四步：配置 GitHub Secrets

### 4.1 打开 GitHub 仓库设置

1. 打开您的 GitHub 仓库页面
2. 点击顶部的 **Settings** 标签

```
┌─────────────────────────────────────────────────────────┐
│ 📦 username/apologize-is-all-you-need                   │
├─────────────────────────────────────────────────────────┤
│ <> Code  Issues  Pull requests  Settings  ← 点击这里    │
└─────────────────────────────────────────────────────────┘
```

### 4.2 进入 Secrets and variables

在左侧菜单中：

```
┌──────────────────────────────────────┐
│ Settings                             │
├──────────────────────────────────────┤
│ General                              │
│ ...                                  │
│ Security                             │
│   ▼ Secrets and variables            │
│     ► Actions         ← 点击这里     │
│     ► Codespaces                     │
│     ► Dependabot                     │
└──────────────────────────────────────┘
```

点击 **Secrets and variables** → **Actions**

### 4.3 添加 Repository Secrets

您会看到：

```
┌─────────────────────────────────────────────────────────┐
│ Actions secrets and variables                           │
├─────────────────────────────────────────────────────────┤
│ ▶ Environment secrets                                   │
│ ▼ Repository secrets                                    │
│                                                         │
│   Secrets are environment variables that are encrypted. │
│   [New repository secret]        ← 点击这个按钮         │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### 4.4 添加第一个 Secret：RENDER_API_KEY

点击 **[New repository secret]** 后：

```
┌──────────────────────────────────────┐
│ Actions secrets / New secret         │
├──────────────────────────────────────┤
│ Name *                               │
│ [________________]                   │
│                                      │
│ Secret *                             │
│ [________________]                   │
│ [________________]                   │
│                                      │
│ [Add secret]  [Cancel]               │
└──────────────────────────────────────┘
```

**填写**：

**Name**（区分大小写，必须完全一致）：
```
RENDER_API_KEY
```

**Secret**（您在第一步复制的 API Key）：
```
rnd_abc123def456ghi789jkl012mno345pqr678stu901
```

点击 **[Add secret]**

---

### 4.5 添加第二个 Secret：RENDER_SERVICE_ID

再次点击 **[New repository secret]**

**Name**：
```
RENDER_SERVICE_ID
```

**Secret**（您在第二步复制的 Service ID）：
```
srv-ctq8p8v79t8c73bqh8rg
```

点击 **[Add secret]**

---

### 4.6 添加第三个 Secret：RENDER_SERVICE_URL

再次点击 **[New repository secret]**

**Name**：
```
RENDER_SERVICE_URL
```

**Secret**（您在第三步复制的 Service URL）：
```
https://apologize-backend-ctq8.onrender.com
```

⚠️ **注意**：
- 必须包含 `https://`
- 末尾**不要**加斜杠 `/`

点击 **[Add secret]**

---

### 4.7 验证 Secrets 已添加

完成后，您应该看到：

```
┌─────────────────────────────────────────────────────────┐
│ Repository secrets                                      │
├─────────────────────────────────────────────────────────┤
│ RENDER_API_KEY          Updated now       [Update] [⋮] │
│ RENDER_SERVICE_ID       Updated now       [Update] [⋮] │
│ RENDER_SERVICE_URL      Updated now       [Update] [⋮] │
└─────────────────────────────────────────────────────────┘
```

✅ **检查清单**：
- [ ] 共有 3 个 secrets
- [ ] 所有名称完全一致（区分大小写）
- [ ] RENDER_API_KEY 以 `rnd_` 开头
- [ ] RENDER_SERVICE_ID 以 `srv-` 开头
- [ ] RENDER_SERVICE_URL 包含 `https://` 且无尾部斜杠

---

## 🧪 第五步：测试 CI/CD（手动触发）

### 5.1 打开 Actions 页面

1. 在 GitHub 仓库页面，点击 **Actions** 标签

```
┌─────────────────────────────────────────────────────────┐
│ 📦 username/apologize-is-all-you-need                   │
├─────────────────────────────────────────────────────────┤
│ <> Code  Issues  Pull requests  Actions  ← 点击这里     │
└─────────────────────────────────────────────────────────┘
```

### 5.2 选择 Deploy to Render workflow

左侧会显示所有 workflows：

```
┌──────────────────────────────────────┐
│ Workflows                            │
├──────────────────────────────────────┤
│ Deploy to Render      ← 点击这个     │
│ Deploy to Vercel                     │
└──────────────────────────────────────┘
```

### 5.3 手动触发部署

点击右上角的 **Run workflow** 按钮：

```
┌─────────────────────────────────────────────────────────┐
│ Deploy to Render                                        │
├─────────────────────────────────────────────────────────┤
│ This workflow has a workflow_dispatch event trigger.    │
│                                                         │
│ [Run workflow ▼]                   ← 点击这里           │
└─────────────────────────────────────────────────────────┘
```

会弹出选项：

```
┌──────────────────────────────────────┐
│ Run workflow                         │
├──────────────────────────────────────┤
│ Branch: main                    ▼    │
│                                      │
│ Deployment environment          ▼    │
│ production                           │
│                                      │
│ [Run workflow]                       │
└──────────────────────────────────────┘
```

**选择**：
- Branch: `main`（或您当前工作的分支）
- Deployment environment: `production`

点击 **[Run workflow]**

### 5.4 观察部署过程

几秒钟后，您会看到一个新的 workflow run 出现：

```
┌─────────────────────────────────────────────────────────┐
│ Deploy to Render                                        │
├─────────────────────────────────────────────────────────┤
│ 🔄 Test CI/CD deployment                               │
│    Running · main · workflow_dispatch                  │
│    ⏱️ 10s                                               │
└─────────────────────────────────────────────────────────┘
```

点击这个 run 查看详情

### 5.5 查看详细执行日志

您会看到 3 个 jobs：

```
┌─────────────────────────────────────────────────────────┐
│ Test CI/CD deployment                                   │
├─────────────────────────────────────────────────────────┤
│ Jobs                                                    │
│                                                         │
│ 🔄 Code Quality Check          Running...              │
│ ⏸️  Deploy to Render           Pending...              │
│ ⏸️  Post-deployment Tests      Pending...              │
└─────────────────────────────────────────────────────────┘
```

**正常流程**：
1. ✅ Code Quality Check（1-2 分钟）
   - TypeScript 类型检查
   - 构建项目

2. ✅ Deploy to Render（2-3 分钟）
   - 触发 Render 部署
   - 等待部署完成
   - 健康检查

3. ✅ Post-deployment Tests（30 秒）
   - 测试 `/api/health`
   - 测试 `/api/health/llm`
   - 测试 `/api/auth/status`

### 5.6 成功标志

全部完成后，您会看到：

```
┌─────────────────────────────────────────────────────────┐
│ Test CI/CD deployment                                   │
│ ✅ Success · main · 5m 23s                             │
├─────────────────────────────────────────────────────────┤
│ Jobs                                                    │
│                                                         │
│ ✅ Code Quality Check          2m 15s                  │
│ ✅ Deploy to Render            2m 30s                  │
│ ✅ Post-deployment Tests       38s                     │
└─────────────────────────────────────────────────────────┘
```

🎉 **恭喜！CI/CD 配置成功！**

---

## 🚀 第六步：测试自动部署

### 6.1 进行一个小的代码修改

让我们做一个测试性修改来验证自动部署：

```bash
# 在项目根目录
cd /home/user/apologize-is-all-you-need

# 修改 backend 的某个文件（例如添加注释）
echo "" >> backend/README.md

# 提交修改
git add backend/README.md
git commit -m "Test CI/CD auto deployment"

# 推送到 main 分支（或合并到 main）
git push origin main
```

### 6.2 自动触发

推送后，GitHub Actions 会**自动检测**到 `backend/**` 路径的变化，并自动触发部署。

```
git push origin main
    ↓
GitHub 检测到 backend/ 目录有变化
    ↓
自动运行 "Deploy to Render" workflow
    ↓
2-5 分钟后部署完成
```

### 6.3 查看自动部署

1. 打开 GitHub 仓库的 **Actions** 页面
2. 您会看到一个新的 workflow run 自动开始：

```
┌─────────────────────────────────────────────────────────┐
│ Deploy to Render                                        │
├─────────────────────────────────────────────────────────┤
│ 🔄 Test CI/CD auto deployment                          │
│    Running · main · push                               │
│    Triggered by push event                             │
└─────────────────────────────────────────────────────────┘
```

注意 trigger 类型是 `push` 而不是 `workflow_dispatch`

---

## ✅ 完成！配置检查清单

确认以下所有项目：

### GitHub Secrets 配置
- [ ] `RENDER_API_KEY` 已添加（以 `rnd_` 开头）
- [ ] `RENDER_SERVICE_ID` 已添加（以 `srv-` 开头）
- [ ] `RENDER_SERVICE_URL` 已添加（包含 `https://`）
- [ ] 所有 Secret 名称完全一致（区分大小写）

### 测试结果
- [ ] 手动触发部署成功（workflow_dispatch）
- [ ] 所有 3 个 jobs 都显示 ✅
- [ ] 自动部署测试成功（push trigger）
- [ ] 健康检查通过

### 预期行为
- [ ] 推送到 `main` 分支自动触发部署
- [ ] 修改 `backend/**` 路径触发部署
- [ ] 修改 `frontend/**` 路径**不**触发后端部署
- [ ] PR 到 `main` 分支运行检查但不部署

---

## 🎯 现在您拥有的能力

### ✅ 自动化部署

**推送代码自动部署**：
```bash
git add .
git commit -m "Add new feature"
git push origin main

# GitHub Actions 自动：
# 1. 检查代码质量 ✓
# 2. 部署到 Render ✓
# 3. 运行健康检查 ✓
# 4. 2-5 分钟后上线 ✓
```

### ✅ 手动触发部署

如果需要立即重新部署（不修改代码）：
1. GitHub → Actions → Deploy to Render
2. Run workflow → 选择 branch 和 environment
3. 立即触发部署

### ✅ 代码质量保证

每次部署前自动运行：
- TypeScript 类型检查
- 项目构建验证
- 单元测试（如果有）

### ✅ 部署验证

每次部署后自动验证：
- 服务健康状态
- LLM 服务可用性
- 认证端点正常

---

## 🔍 监控和日志

### 查看部署历史

**GitHub Actions**：
- 访问：`https://github.com/<username>/apologize-is-all-you-need/actions`
- 查看所有部署记录、执行时间、成功/失败状态

**Render Logs**：
- Dashboard → 您的服务 → Logs
- 查看服务运行日志、错误信息

### 接收通知

GitHub Actions 会在以下情况发送通知（如果启用了 GitHub 通知）：
- ✅ 部署成功
- ❌ 部署失败
- ⚠️ 检查失败

---

## 🚨 常见问题排查

### 问题 1：Workflow 没有自动触发

**症状**：推送代码到 main 但没有运行 workflow

**原因**：
1. 修改的文件不在触发路径中
2. Workflow 文件本身有语法错误

**解决方法**：
```bash
# 检查修改的文件路径
git diff --name-only origin/main

# 应该包含 backend/** 路径的文件
# 例如：backend/src/server.ts
```

如果只修改了 `frontend/**` 或 `docs/**`，不会触发后端部署（这是正常的）

---

### 问题 2：Code Quality Check 失败

**症状**：
```
❌ Code Quality Check    Failed
```

**原因**：TypeScript 类型错误或构建失败

**解决方法**：
1. 查看具体错误日志（点击 job 查看详情）
2. 在本地运行相同的检查：
   ```bash
   cd backend
   npm install
   npx tsc --noEmit  # TypeScript 检查
   npm run build     # 构建检查
   ```
3. 修复错误后重新提交

---

### 问题 3：Deploy to Render 失败 - 401 Unauthorized

**症状**：
```
❌ Deploy to Render    Failed
Error: 401 Unauthorized
```

**原因**：RENDER_API_KEY 无效或已过期

**解决方法**：
1. 重新生成 Render API Key
2. 更新 GitHub Secret：
   - Settings → Secrets and variables → Actions
   - 找到 RENDER_API_KEY
   - 点击 Update
   - 输入新的 API Key
3. 重新运行 workflow

---

### 问题 4：Health Check 超时

**症状**：
```
❌ Check deployment health    Failed
⏳ Waiting for service... (attempt 10/10)
```

**原因**：
1. Render 服务部署失败
2. 健康检查端点不可用
3. 免费计划服务正在从休眠中唤醒（需要更长时间）

**解决方法**：
1. 访问 Render Dashboard → Logs 查看部署日志
2. 检查是否有构建错误或启动错误
3. 手动访问健康检查 URL：
   ```bash
   curl https://your-service.onrender.com/api/health
   ```
4. 如果是休眠问题，可以增加 workflow 中的等待时间：
   - 编辑 `.github/workflows/deploy-render.yml`
   - 修改 `sleep 60` 为 `sleep 120`（等待 2 分钟）

---

### 问题 5：Post-deployment Tests 失败

**症状**：
```
✅ Code Quality Check    Success
✅ Deploy to Render      Success
❌ Post-deployment Tests Failed
```

**原因**：端点返回的响应不符合预期

**解决方法**：
1. 查看测试日志中的实际响应
2. 手动测试端点：
   ```bash
   curl https://your-service.onrender.com/api/health
   curl https://your-service.onrender.com/api/health/llm
   ```
3. 如果 LLM 健康检查失败，检查 Render 环境变量中的 LLM API Key

---

## 📚 相关文档

- [Render 首次配置指南](./RENDER_FIRST_TIME_SETUP.md) - 手动部署详细步骤
- [部署概览](./DEPLOYMENT_OVERVIEW.md) - 部署方案对比
- [问题排查](./TROUBLESHOOTING.md) - 常见问题解决

---

## 🎉 恭喜！

您已成功配置 CI/CD 自动部署！

**现在的工作流**：
```
编写代码 → git push →
  ↓
自动检查 → 自动部署 → 自动测试 →
  ↓
2-5 分钟后新版本上线 ✅
```

**节省时间**：
- 手动部署：每次 5-10 分钟
- 自动部署：每次 0 分钟（全自动）
- **时间节省：100%** 🎊

---

**享受自动化部署的便利！** 🚀

---

**文档版本**：v1.0
**最后更新**：2025-11-15
**适用项目**：Apologize-is-all-you-need
