{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "name": "apologize-is-all-you-need",
  "buildCommand": "cd frontend && npx vite build",
  "outputDirectory": "frontend/dist",
  "installCommand": "mv package.json package.json.bak && cd frontend && rm -rf node_modules && npm install --legacy-peer-deps && cd .. && mv package.json.bak package.json",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
